# Optional: OpenAI-compatible API for live chapter generation.
# Without these, the local demo writer is used automatically.

OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
